interface:
  display_name: "Learning Supervisor"
  short_description: "Teach while coding with step, action, proof, why, and next guidance."
  default_prompt: "learning mode chain: run this task while teaching in parallel. For each meaningful step output Step, Action, Proof, Why, and Next. Keep explanations plain-English and concise, preserve UX/command names unless approved, run frequent checks, stop on hard failures with one fallback, and end with npx handoff-agent instructions."
